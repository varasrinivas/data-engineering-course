Run `python scripts/build.py` (full rebuild: data + engines + traces + all fragments,
node --check, PROGRESS.md refresh). Summarize the coverage report: modules missing
analogy/javaBridge/labs/checks, and any script-block syntax failures with fixes.
