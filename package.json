{
  "name": "de-pyspark-course",
  "version": "1.0.0",
  "private": true,
  "description": "From Backend to Big Data — Data Engineering with PySpark. Single-file browser course player.",
  "scripts": {
    "build": "python scripts/build.py",
    "validate": "python scripts/validate.py --all --final",
    "qa": "node scripts/qa.js",
    "smoke": "node scripts/smoke_dom.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "comment": "jsdom is used only by scripts/qa.js and scripts/smoke_dom.js to mount the lab engines in a headless DOM. The shipped player (player/index.html) has zero runtime dependencies.",
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
